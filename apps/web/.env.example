# Copy to .env for local Prisma / Next.js

# Dev: Postgres on bnkr-bunsen01 (VLAN 326), reachable from TESLA-GRID.
DATABASE_URL="postgresql://ssr:CHANGEME@192.168.78.136:5432/savage_sun_rising?schema=public"

# Prod (on the app host): loopback, or the Unix socket — no password needed.
# DATABASE_URL="postgresql://ssr@localhost/savage_sun_rising?host=/var/run/postgresql&schema=public"

# Azure (Master's project): same schema, only the DSN and TLS mode change.
# DATABASE_URL="postgresql://ssr:PASS@ssr-pg.postgres.database.azure.com:5432/savage_sun_rising?sslmode=require&schema=public"

# Corpora (not in this repo). Used by scripts/import-corpora.mjs when the
# --spells / --monsters flags are omitted.
SSR_SPELLS_FILE="C:\\jdlcode\\repos\\Depp-Magic\\Reference\\spell-powers-v3.6.2.json"
SSR_MONSTERS_FILE="C:\\jdlcode\\Claude\\SavageSun\\monsters.json"
