# @savage-sun-rising/schema

Shared Zod / TypeScript types for characters, spell-powers, and monster_index-shaped rows.
Vocabulary aligns with Depp-Magic 3.6 overlays (`ds`, `2e`, `2e-rev`, `5e`).

Mirrored into `apps/web/src/schema` for zero-config imports in the Next app.
