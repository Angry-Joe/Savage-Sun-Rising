# DEPRECATED — Legacy Blazor master's prototype

This `DarkSun.Web` Blazor + MudBlazor application (and the related
`DarkSun.*` .NET projects) is **deprecated and not used** by the product app.

- **Do not extend** MudBlazor UI, AWS Cognito, DynamoDB, or Azure wiring here.
- The product lives under **`apps/web`** (Next.js + TypeScript + Postgres/Prisma).
- Kept in-tree temporarily for reference only; removal is a later cleanup task.

See the repository root `README.md` for how to run the new stack.
